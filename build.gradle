plugins {
    id "org.sonarqube" version "2.8"
}

apply plugin: "org.sonarqube"

sonarqube {
    properties {
     property "sonar.sources", "src"
     property "sonar.exclusions", "**/webpush.js, **/service-worker.js, **/*.yaml"
    }
}